services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NODE_ENV: ${NODE_ENV:-production}
    container_name: portfolio-web
    ports:
      - "${PORT:-8020}:80"
    restart: unless-stopped
